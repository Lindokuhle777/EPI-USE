export default class employee {
    constructor(node) {
      this.firstName = node.first_name;
      this.children = node.children;
      this.empNum = node.empNum;
      this.position = node.position;
      this.lastName = node.last_name;
      this.email = node.email;
      this.salary = node.salary;
      this.manager = node.manager;
      this.DOB = node.DOB;
      this.children = node.children
    }
  
    
  }